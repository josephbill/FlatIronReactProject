import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; //this is where we will create our compinents 


ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

